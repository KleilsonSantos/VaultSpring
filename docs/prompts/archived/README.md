# Archived prompts

Retired `{slug}.vN.md` assets live here. Prefer bumping `version` and moving the previous file here rather than deleting history.

Update `index.yaml`: set `status: archived` or remove the row and add a note in the replacement asset's `related_prompts`.
